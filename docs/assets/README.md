# Vbot Documentation Assets

This folder is for README and documentation visuals only.

Do not put runtime assets here. Character model files, backgrounds, reference audio, and generated samples should stay in their existing runtime folders under `asset/`.

## Existing Screenshot

The main README preview currently uses:

- `asset/screenshots/interface.png`

That file is already a clean app screenshot, so do not duplicate it into `docs/assets/readme/` unless replacing it with a newer polished screenshot.

## Generated Diagrams

These diagrams are generated documentation assets:

- `animation/pose-vector-flow.svg`
- `animation/tha4-siren-morpher-flow.svg`
- `llm/character-hotswap-flow.svg`
- `memory/runtime-cache-boundary.svg`
- `tts-data/styletts2-data-pipeline.svg`
- `evaluation/evaluation-workflow.svg`

## User-Captured Assets To Add Later

Add real screenshots and short clips here when they are ready:

- `readme/app-overview.gif` or `readme/app-overview.mp4`
- `animation/avatar-idle.gif`
- `animation/avatar-speaking.gif`
- `animation/emotion-states.png`
- `tts-data/segment-reviewer.png`
- `evaluation/tts-eval-form.png`

Keep GIFs short and small. Larger videos should be uploaded to GitHub Releases or another external host and linked from the docs.
